%br/
.opaque-wrap
  .standard.standard-header
    %strong.muted  Képek
    .right
      - if can? :update, @profile
        = link_to "#myModal", "data-toggle" => "modal", title: "Új kép hozzáadása", class: "opaque" do
          Új kép
  .standard
    .pictures
      - if @photos.any?
        - @photos.each do |photo|
          = link_to profile_photos_path(@profile) do
            = image_tag(photo.image_url(:thumb), class: "photo-thumb", title: photo.name) if photo.image_url
      - else
        %strong.pagination-centered.muted Tölts fel képeket.