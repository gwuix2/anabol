#upload_photo.reveal-modal
  .modal-header
    %h3 Képfeltöltés
  .modal-body
    = simple_form_for([@profile, @new_photo], :html => { :class => 'form-horizontal' }) do |f|
      .form-inputs
        = f.input :name, label: "Kép neve"
        = f.input :image, label: "Képfájl"
        = f.input :keszult, label: "Készült"
        = f.input :description, label: "Leírás", :input_html => { rows: 3, class: "span10"}
        = f.input :testsuly, label: "Testsúly(kg)"
        = f.input :testzsir, label: "Testzsír(%)"
      .form-actions
        = f.submit "Feltöltés", class: "button expand"
  %a.close-reveal-modal ×